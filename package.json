{
  "name": "langame-api",
  "version": "1.0.5",
  "dependencies": {
    "@redocly/openapi-cli": "1.0.0-beta.94"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi lint"
  }
}
